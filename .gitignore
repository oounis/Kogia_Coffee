# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Office lock/temp files
~$*
*.tmp

# Python
venv/
__pycache__/
*.pyc

# Legacy binary docs (kept locally, not in repo history)
*.docx

# Node (if added later)
node_modules/
